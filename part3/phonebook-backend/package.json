{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook app backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "\"echo \\\"Error: no test specified\\\" && exit 1\"",
    "build:ui": "if exist ..\\phonebook-backend\\dist rd /s /q ..\\phonebook-backend\\dist && cd ..\\phonebook-frontend && if exist dist rd /s /q dist && npm run build && xcopy dist ..\\phonebook-backend\\dist /E /I /Y",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Mahmoud Ahmed",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^8.20.0",
    "morgan": "^1.10.1"
  }
}
